// Group and version are set in gradle.properties and propagated here.
// Convention plugins (build-logic) handle per-module configuration.
allprojects {
    group = rootProject.group
    version = rootProject.version
}
